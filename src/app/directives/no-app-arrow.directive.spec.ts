import { NoAppArrowDirective } from './no-app-arrow.directive';

describe('NoAppArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new NoAppArrowDirective();
    expect(directive).toBeTruthy();
  });
});
